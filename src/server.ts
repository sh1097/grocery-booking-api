import BookingModel, { Booking } from 'src/models/booking.model';

export const createBooking = async (userId: string, items: string[]): Promise<Booking> => {
    // Implement logic to create a booking
};
